import AdBanner from "./AdBanner";

/**
 * Placeholder for future affiliate partner links.
 * Currently renders an additional AdSense unit.
 */
export default function AffiliatesSidebar() {
  return <AdBanner format="auto" className="rounded-xl overflow-hidden" />;
}